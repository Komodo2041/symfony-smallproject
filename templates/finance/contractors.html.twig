 
        {% extends 'finance/financemain.html.twig' %}

        {% block body %}
            <br/>
            <h2>Kontraktorzy</h2>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Name</th> 
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for contractor in contractors %}
                        <tr> 
                            <td>{{ contractor.id }}</td>
                            <td>{{ contractor.name }}</td>
                            <td>
                                <a href="/contractor/edit/{{ contractor.id }}" > <i class="fa fa-edit " aria-hidden="true"></i> </a>
                                <a href="/contractor/delete/{{ contractor.id }}" class="delconfirm"> <i class="fa fa-trash " aria-hidden="true"></i> </a>    
                            </td>
                        </tr>
                {% else %}
                    <td colspan="2">Brak Kontraktorów</li>
                {% endfor %}
                </tbody>
            </table>
            <a href="/contractor/add" class="btn btn-info">Dodaj Kontraktora</a><br/>
        {% endblock %}
 

 